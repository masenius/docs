##### Installation

##### Useful commands
`docker -version`
`docker pull`
`docker run`
`docker ps`
`docker ps -a`
`docker images`
`docker rm`
`docker rmi`

##### Build & Push
`docker build`
`docker push`
